import React from "react";
import "./styles.css";
import { db } from "./../../config/db";

const Table = () => {
  /**
   * What do we need => display upcoming matches from db.json
   */
  return <div>Table</div>;
};

export default Table;
